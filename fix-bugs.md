### Need to ->

- Sort just by category + updatedAt (desc/asc)
- Sort just by category + price (desc/asc)

# Need to get rid of Scan Command & migrate to Query + Sort By + GSI

# AddBooks Page typescript issue

# BookFormPartOne --> Fix uncontrolled field issues + validation error messages + isbn schema validation

# Need to lock submit and display a spinner while the form is being submitted

# Need to fix the pagination related bugs

# Continue with next set of admin features + Frontend deployment to cloudfront + s3

# Add authentication and authorization using Cognito
